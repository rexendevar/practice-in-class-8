name: Hello World flynn

# Events

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow_dispatch:


# Workflow / jobs

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello
        run: echo "Hello World 1"
        shell: bash
  hello2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello2
        run: |
          uptime
          echo "hello world 2"
        shell: bash