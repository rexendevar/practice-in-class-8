name: Hello World

# Events

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

  workflow-dispatch:


# Workflow / jobs

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hello
        run: echo "hello world"
        shell: bash